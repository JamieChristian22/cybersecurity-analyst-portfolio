index=auth action=failure
| bin _time span=10m
| stats count AS failures dc(username) AS distinct_users values(username) AS targeted_users BY _time src_ip
| where distinct_users >= 5 AND failures >= 5
| eval severity="high"
